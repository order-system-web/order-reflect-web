/**
 * このフォルダの index.html の接続先。
 *
 * ここへ、GASのウェブアプリのデプロイURL（末尾が /exec のもの）を貼ること。
 *   例）var MR_API_URL = 'https://script.google.com/macros/s/xxxxxxxx/exec';
 *
 * 置き場所ごとに値が違うので、**このリポジトリには実物のURLを入れない**。
 * 空のまま配ると、画面は「接続先が設定されていません（config.js）」と出す。
 *
 * 手順の全体は docs/mobile-app-static-hosting.md を見ること。
 */
var MR_API_URL = '';
